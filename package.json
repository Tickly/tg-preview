{
  "name": "tg-preview",
  "version": "0.1.2",
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build --target lib --name tg-preview ./src/index.js",
    "lint": "vue-cli-service lint",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "photoswipe": "^4.1.3"
  },
  "devDependencies": {
    "@vue/cli-service": "^4.5.4",
    "vuepress": "^1.5.4"
  },
  "main": "dist/tg-preview.common.js"
}
